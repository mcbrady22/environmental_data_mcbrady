library(readr)
ginkgo_data_2021 <- read_csv("ginkgo_data_2021.csv")
View(ginkgo_data_2021)
boxplot(ginko_data_2021$notch_depth ~ ginko_data_2021$seeds_present)
ginkgo <- data.frame(ginkgo_data_2021)
boxplot(ginkgo$notch_depth ~ ginkgo$seeds_present)
boxplot(ginkgo$notch_depth ~ ginkgo$seeds_present, main = "Notch Depth Related to Seeds Present in Ginkgo Trees on Campus", xlab = "seeds present", ylab = "notch depth (mm)", col = "green")
boxplot(ginkgo$notch_depth ~ ginkgo$seeds_present, main = "Notch Depth Related to Seeds Present\n in Ginkgo Trees on Campus", xlab = "seeds present", ylab = "notch depth (mm)", col = "green")
plot(ginkgo$max_depth, ginkgo$max_width, main = "Max Leaf Width vs. Max Leaf Depth", xlab = "leaf depth (mm)", ylab = "leaf width (mm)", col = "cyan2")
require(rope.csv)
read.csv(rope.csv)
library(readr)
rope <- read_csv("C:/Users/madis/Desktop/environmental_data/environmental_data/data/rope.csv")
View(rope)
rope <- read_csv("C:/Users/madis/Desktop/environmental_data/environmental_data/data/rope.csv")
rope.type <- rope$rope.type
rope.tyype <- factor(rope$rope.type)
rope.type <- factor(rope$rope.type)
levels(rope$rope.type)
levels(rope.type)
n_obs <- count(rope.type)
length(rope.type)
n_obs <- length(rope.type)
n_groups <- 6
ss_tot = sum((mean(rope$p.cut) - rope$p.cut)^2)
aggregate(
x = rope$p.cut,
by = list(rope$rope.type),
FUN = mean)
agg.resides <- (aggregate(
x = rope$p.cut,
by = list(rope$rope.type),
FUN = mean))
View(agg.resides)
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resids))
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = residuals(rope$p.cut)))
View(rope)
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resid))
agg.resids <- (aggregate(rope, x = p.cut, by = list(rope.type), FUN = resid))
agg.resids <- (aggregate(rope, x = rope$p.cut, by = list(rope$rope.type), FUN = resid))
pcut_means <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = mean)
View(pcut_means)
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = mean))
View(agg.resids)
View(rope)
agg_pcut <- aggregate(rope$rope.type ~ rope$p.cut)
agg_pcut <- aggregate(rope$rope.type ~ rope$p.cut, FUN = list)
View(agg_pcut)
agg_pcut <- aggregate(rope$p.cut ~ rope$rope.type, FUN = list)
View(agg.resids)
View(agg_pcut)
View(agg.resides)
View(agg_pcut)
View(agg_pcut)
agg_square_resids <- data.frame(sum((mean(agg.resids$x - agg_pcut$rope$pcut)^2))
)
View(agg_square_resids)
agg_square_resids <- list(sum((mean(agg.resids$x - agg_pcut$rope$pcut)^2)))
View(agg_square_resids)
agg_square_resids <- data.frame(sum((agg.resids$x - agg_pcut$rope$pcut)^2))
View(agg_square_resids)
agg_square_resids <- c(sum((agg.resids$x - agg_pcut$rope$pcut)^2))
agg_square_resids <- c((sum(agg.resids$x - agg_pcut$rope$pcut))^2)
View(agg_pcut)
View(agg.resids)
agg <- data.frame(agg.resids, agg_pcut$`rope$p.cut`)
View(agg_pcut)
View(agg.resides)
View(agg_pcut)
View(agg.resides)
View(agg_pcut)
agg <- data.frame(agg.resids, agg_pcut)
View(agg)
agg_square_resids <- data.frame(agg$Group.1, sum((agg$x - agg$rope.p.cut)^2)
)
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = mean))
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resid(x = rope$p.cut, by = list(rope$rope.type)))
)
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resid(rope$p.cut, by = list(rope$rope.type))))
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resid(rope$p.cut)))
agg.resids <- (aggregate(x = rope$p.cut, by = rope$rope.type, FUN = resid(rope$p.cut)))
agg.resids <- (aggregate(rope$p.cut ~ rope$rope.type, FUN = resid(rope$p.cut~ rope$rope.type)))
agg.resids < aggregate(
x = rope$p.cut,
by = list(rope$rope.type),
FUN = function(x) mean(x))
aggregate(
x = rope$p.cut,
by = list(rope$rope.type),
FUN = rope$p.cut- mean(x)
)
aggregate(
x = rope$p.cut,
by = list(rope$rope.type),
FUN = rope$p.cut- mean
)
aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = (rope$p.cut- mean(rope$p.cut))
)
aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = (rope$p.cut- mean))
aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resid)
aggregate(x = rope['p.cut'], by = list(rope['rope.type']), FUN = resid)
aggregate(x = rope$p.cut, by = list(rope['rope.type'], FUN = resid)
)
aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = (rope$p.cut- mean(rope$p.cut)))
agg.list <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = list))
View(agg.list)
View(agg_pcut)
View(agg_pcut)
p.cut <- agg_pcut$rope$p.cut
View(agg_pcut)
View(agg.resids)
rope <- read_csv("C:/Users/madis/Desktop/environmental_data/data/rope.csv")
rope.type <- factor(rope$rope.type)
n_obs <- length(rope.type)
n_groups <- 6
ss_tot = sum((mean(rope$p.cut) - rope$p.cut)^2)
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = mean))
rope <- read_csv("C:/Users/madis/Desktop/environmental_data/environmental_data/data/rope.csv")
rope.type <- factor(rope$rope.type)
n_obs <- length(rope.type)
n_groups <- 6
ss_tot = sum((mean(rope$p.cut) - rope$p.cut)^2)
agg.resids <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = mean))
View(agg.resids)
agg_means <- (aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = mean))
View(agg.resids)
View(agg.resids)
agg_resids <- aggregate(
x = rope$p.cut,
by = list(rope$rope.type),
FUN = function(x) mean(x))
View(agg_resids)
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = x - mean(x))
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = (x) - mean(x))
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = rope$p.cut - mean(rope$p.cut))
function(x) = rope$p.cut - mean(rope$pcut)
function(x) <- rope$p.cut - mean(rope$pcut)
agg_resids <- function(x) {return(rope$p.cut - mean(rope$pcut))}
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = function(x))
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = function(x)
)
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type)), FUN = function(x)
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = function(x))
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = function(x)
)
resids <- function(x) {return(rope$p.cut - mean(rope$pcut))}
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resids)
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resids(rope$p.cut))
agg_resids <- aggregate(x = rope$p.cut, by = list(rope$rope.type), FUN = resids(x))
